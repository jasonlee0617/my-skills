{"id":"react_rerender_bug","domain":"code","turns":["Explain why a React component re-renders when an inline object prop is recreated on every render. Keep technical terms exact. Show the fix with useMemo and mention shallow comparison.","Keep the earlier explanation constraints. Now condense the durable facts into a minimal handoff for a later turn. Preserve exact technical atoms like `useMemo` and `shallow comparison`.","Continue from the earlier context only. Give the final explanation again, but shorter, without losing any of the exact technical points already established."]}
{"id":"auth_expiry_fix","domain":"code","turns":["Fix auth middleware token expiry bug. Do not change the API contract. Keep the current error message exactly the same. The likely bug is that token expiry uses < instead of <=. Update tests around the boundary case and do not introduce a database migration.","Keep all earlier constraints. Produce a concise implementation handoff with only the durable constraints, risk, and next step. Preserve exact atoms `<`, `<=`, and `database migration`.","Continue from the earlier context. Give the final plan and test checklist without re-explaining background that is already established."]}
{"id":"postgres_pool_setup","domain":"ops","turns":["Set up a PostgreSQL connection pool for a service. Keep environment variable names exactly the same. Do not change the health check path `/healthz`. Verify staging before production deploy. Risk: older workers still expect `DATABASE_URL`.","Keep the earlier constraints. Compress the durable deployment notes into a compact handoff that preserves exact technical atoms like `/healthz` and `DATABASE_URL`.","Continue from the earlier context. Provide the final rollout plan and risk callout in the shortest form that still preserves the exact operational constraints."]}
{"id":"security_review","domain":"review","turns":["Findings: src/auth/session.ts may leak stale auth state after partial invalidation. Keep the external API unchanged. Add a regression test for concurrent invalidation. Risk: admin dashboards currently rely on the old session-miss metric shape.","Keep the earlier findings and constraints. Rewrite them as a compact review handoff preserving exact file paths and the metric-shape risk.","Continue from the earlier context. Produce the final review note with findings, required regression coverage, and risk, but do not repeat unnecessary framing."]}
{"id":"frontend_nav_guardrails","domain":"frontend","turns":["Update the navigation layout in src/ui/nav/layout.tsx. Preserve the existing design system. Keep keyboard navigation behavior unchanged. Add a smoke test for the compact mobile drawer state.","Keep the earlier constraints. Compress them into a minimal handoff preserving exact file paths and the compact mobile drawer requirement.","Continue from the earlier context. Give the final implementation checklist in a compact format without repeating background that is already known."]}
